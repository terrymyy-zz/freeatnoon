<!DOCTYPE html>
<html lang="en">
  <head>
    <title>HUNTER > HONG</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= favicon_link_tag "FreeAtNoon_favicon.ico", 'data-turbolinks-track' => true  %>
    <link type="image/x-icon" rel="shortcut icon" href="FreeAtNoon_favicon.ico">
    <link href='http://fonts.googleapis.com/css?family=Roboto:400,300,100' rel='stylesheet' type='text/css'>
    <%= csrf_meta_tags %>

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-50721642-1', 'freeatnoon.com');
      ga('send', 'pageview');
    </script>

    <style>
    @media (max-width: 992px) {
     footer {
       display: none;
     }
    }
    @media (max-width: 768px) {
      p{
        font-weight: 300 !important;
      }

      #bigBox {
        margin-top: 0px !important;
        padding-top: 20px;
      }

      #time-info-form > h2 {
        font-size: 14pt;
      }
      #time-info-form-answers-bottom > input > #leaving-date{
        width: 140px;
      }
      #welcome-message > .btn {
        width: 200px;
        padding-left: 5px;
        padding-right: 5px;
        font-size: 18pt;
        font-weight: 300;
      }
      #time-info-form > .btn {
        width: 200px;
        padding-left: 5px;
        padding-right: 5px;
        font-size: 16pt;
        font-weight: 300;
      }

      input[type="submit"] {
        width: 200px;
        padding-left: 5px !important;
        padding-right: 5px;
        font-size: 16pt;
        text-align: center;
        font-weight: 300;
      }

      #time-info-form {
        width: 280px;
      }

      #user-info-form > h2 {
        font-size: 18pt;
      }

      #home_img {
      background-position: right;
      }

      label {
        font-weight: 300 !important;
      }

      #time-info-form-answers-top {
        font-weight: 300 ;
      }

    }

    </style>
  </head>

  <body>
    <% if notice %>
      <p id="notice" class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p id="alert" class="alert"><%= alert %></p>
    <% end %>
    <% if current_user %>
      <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
    <% end %>

    <% if params[:controller] != "users" %>
      <div id="home_img">
        <div class="container">
          <div class="row">
            <div id="bigBox" class="col-lg-6 col-lg-offset-3 col-md-8 col-md-offset-2 col-sm-12 col-xs-12">
              <div id="bigBoxContent">
                <%= yield %>
              </div>
              <div class="row">
                <footer>Made by Penn students.</footer>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
