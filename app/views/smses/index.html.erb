<div class="container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <h1> Smses Admin Page </h1>
      <table class="table table-striped">
	<thead>
          <tr>
            <th>At</th>
            <th>From</th>
            <th>To</th>
            <th>Body</th>
          </tr>
	</thead>
	<tbody>
          <% @smses.each do |sms| %>
          <tr>
            <td class="ftime"><%= sms.created_at.strftime("%b %d %I:%M%p") %></td>
            <td><%= sms.from_user ? sms.from_user.full_name : sms.from %></td>
            <td><%= sms.to_user ? sms.to_user.full_name : sms.to %></td>
            <td><%= sms.body %></td>
	    </td>
          </tr>
          <% end %>
	</tbody>
      </table>
    </div>
  </div>
</div>
